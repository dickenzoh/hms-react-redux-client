import React from "react";
import { TextField } from "@mui/material";

const Searchbar = () => {
  return <TextField fullWidth label="Search by patient name" id="fullWidth" />;
};

export default Searchbar;
